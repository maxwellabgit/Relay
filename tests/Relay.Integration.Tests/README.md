# Relay.Integration.Tests scaffold (§13).
# Add end-to-end fixture scenarios here as cloud gates expand.
# Currently empty — verify-cloud.sh treats missing csproj as soft-pass scaffold.

# Intentionally no project file yet; Core+Gateway+DevHarness cover required gates.
