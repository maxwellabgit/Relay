# QUESTIONS.md

Open items that need a human decision (not guessable from the repo):

1. **Local model endpoint (Windows live gate)** — Which host/port/model should live Slice 4–7 runners use (e.g. Ollama OpenAI-compatible URL + model name)?
2. **Wispr Flow** — Is a Wispr Flow capture path available on the Windows verification machine for listening/live gates?
3. **PR collaborator access** — Who should be added as reviewers/collaborators on the vNext PR besides the author?
4. **Desktop wiring next** — Should the next pass be a Windows agent binding `MainWindow` to `IRelaySurface` / `CaseRuntimeSurface`, or stay on Core until more CaseRuntime parity lands?
