/** Re-export recorder from diagnostics package path; keep root module for compatibility. */
export {
  RuntimeRecorder,
  buildRunManifest,
  type RuntimeRecorderDeps,
} from "./diagnostics/RuntimeRecorder.js";
