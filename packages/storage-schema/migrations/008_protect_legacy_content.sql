-- Marker: legacy plaintext learning columns scrubbed into protected artifacts.
-- Data migration runs in store open (Node ArtifactStorePort / Rust DPAPI put).
-- Columns already exist from 006_protected_learning.sql.

SELECT 1;
